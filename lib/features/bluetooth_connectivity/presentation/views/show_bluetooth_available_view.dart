import 'package:bleutooth_with_esp8266/features/bluetooth_connectivity/presentation/views/widgets/show_bluetooth_available_view_body.dart';
import 'package:flutter/material.dart';

class ShowBluetoothAvailableView extends StatelessWidget {
  const ShowBluetoothAvailableView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MyApp(),
    );
  }
}